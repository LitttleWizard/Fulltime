/**
 * Supabase project details. Both values are PUBLIC by design — the anon key
 * identifies the project, not a user, and row-level security is what protects
 * the data. See supabase/README.md.
 *
 * Leave these empty to run the site with positions in localStorage only.
 * Never put the service_role key here: it bypasses RLS.
 */
window.SUPABASE_URL = '';
window.SUPABASE_ANON_KEY = '';
